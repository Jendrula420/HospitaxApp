@model IEnumerable<HospitaxApp.Models.Hospitujacy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Protokoly.Kurs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prowadzacy.Imie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Protokoly.Kurs)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prowadzacy.Imie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HospitujacyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HospitujacyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HospitujacyID })
        </td>
    </tr>
}

</table>
